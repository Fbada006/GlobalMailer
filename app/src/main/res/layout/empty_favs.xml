<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/about_gradient_background"
    android:orientation="vertical"
    android:padding="@dimen/error_layout_padding">

    <com.airbnb.lottie.LottieAnimationView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:layout_weight="2"
        android:padding="@dimen/error_layout_padding"
        android:scaleType="centerCrop"
        app:lottie_autoPlay="true"
        app:lottie_fileName="empty_table.json"
        app:lottie_loop="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="@dimen/error_layout_padding"
        android:text="@string/nothing_to_see_here_at_the_moment"
        android:textColor="@android:color/black"
        android:textSize="@dimen/loading_text_size"
        android:textStyle="bold" />

</LinearLayout>